<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OnlineSchool" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your light theme here. -->

        <!-- Activity/Fragment background color -->
        <item name="android:colorBackground">@color/dark</item>
        <!-- Bottom Menu selector color -->
        <item name="colorSecondaryContainer">@color/light_gray</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/dark</item>

    </style>

    <style name="Theme.OnlineSchool" parent="Base.Theme.OnlineSchool" />
</resources>